# Regulamento Geral da Formula Student FEUP - editable LaTeX

This version keeps the fixed visual layout of the original PDF, but the table of contents has been corrected: dot leaders and page numbers are generated by macros using `\pageref` labels placed on the real pages.

Compile with:

```bash
make
```

The document must be compiled at least twice so the TOC page references resolve.
